import { AppBarProps } from '@mui/material';
import { BaseComponentProps } from '../../types';

export declare type BaseAppBarProps = {
  position?: AppBarProps['position'];
} & BaseComponentProps;

export { default as EdgeAppBar } from './EdgeAppBar';
