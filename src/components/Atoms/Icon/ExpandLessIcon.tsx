import { MdOutlineExpandLess } from 'react-icons/md';
import { BaseIconProps } from '.';

const ExpandLessIcon: React.FC<BaseIconProps> = ({ size }) => {
  return <MdOutlineExpandLess size={size} />;
};

export default ExpandLessIcon;
