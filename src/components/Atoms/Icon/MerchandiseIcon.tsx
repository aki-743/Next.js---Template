import { BaseIconProps } from '.';
import { SiHackthebox } from 'react-icons/si';

const MerchandiseIcon: React.FC<BaseIconProps> = ({ size }) => {
  return <SiHackthebox size={size} />;
};

export default MerchandiseIcon;
