import { AiOutlineAreaChart } from 'react-icons/ai';
import { BaseIconProps } from '.';

const ChartIcon: React.FC<BaseIconProps> = ({ size }) => {
  return <AiOutlineAreaChart size={size} />;
};

export default ChartIcon;
