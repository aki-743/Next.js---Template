import { CheckboxProps } from '@mui/material';
import { BaseComponentProps } from '../../types';

export declare type BaseCheckboxProps = CheckboxProps & BaseComponentProps;

export { default as PrimaryCheckbox } from './PrimaryCheckbox';
