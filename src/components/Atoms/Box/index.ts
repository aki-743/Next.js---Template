import { BaseComponentProps } from '../../types';

export declare type BaseBoxProps = {
  /** HTMLタグ名 */
  component?: any;
} & BaseComponentProps;

export { default as DashedBox } from './DashedBox';
