import React from 'react';
import { BaseLabelProps } from '../../Atoms/Label';

export declare type LabelWithValueProps = {
  /** バリュー */
  value?: React.ReactNode;
} & BaseLabelProps;

export { default as LabelWithValue } from './LabelWithValue';
